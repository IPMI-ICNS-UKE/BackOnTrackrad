from setuptools import setup, find_packages

setup(name='dam4sam',
      version='0.1',
      author='',
      author_email='',
      packages=find_packages())

# setup(name='sam2',
#       version='0.1',
#       author='',
#       author_email='',
#       packages=find_packages())